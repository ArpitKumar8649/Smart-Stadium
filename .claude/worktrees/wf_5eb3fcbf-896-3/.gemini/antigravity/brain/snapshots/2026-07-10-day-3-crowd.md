# Day 3 snapshot — 2026-07-10

_Crowd simulator + routing integration + closed D2 follow-ups._

## Shipped

### D3.1 — closed two D2 follow-ups
- `find_nearest` now takes a `dietary` filter (halal | vegetarian), plumbed through
  `nearestNodesByType` (loader) and the tool schema. Only applies to `concession`.
  Graceful "no halal concession near X" when nothing matches.
- `astar.test.ts` written (was lost when the D2 subagent died). 6 vitest cases:
  fastest-takes-stairs, step_free-detours-via-elevator, no-path→null, step-free warning
  when forced onto stairs, low_crowd penalty applied, trivial same-node route.

### D3.2 — crowd simulator (ADR 0005 + 0008)
- `services/crowd/simulator.ts`. Zones derived from the REAL graph by (level × kind):
  gates/concourse/restrooms/food/seating → 30 zones for MetLife.
- Continuous matchday curve via per-kind keyframes + linear interp + small
  deterministic per-zone wobble. Phases: pre_match / first_half / halftime /
  second_half / post_match.
- Verified curve shape: halftime restroom surge to ~0.93 (4+ min wait), seating
  inverts between halves, gates spike for egress.
- T+15 / T+30 projections per zone with confidence (drops near volatile
  transitions — e.g. 0.45 approaching halftime). source: "sim" | "injected".
- Match clock (`getSimMinute`/`setSimMinute`/`setSpeed`) so demo/admin can jump
  phases. Defaults to minute 40 (just before halftime) so first view is lively.
- Admin `override(zone, density, wait, ttl)` → source "injected", auto-expires.
  Powers the /admin wow-moment.
- `crowdPenaltyForNode(id)` → density²·150 seconds, consumed by A* `low_crowd`.
- 7 vitest cases guard the curve shape + projections + override + penalty.

### D3.3 — crowd into routing + concierge
- `find_route` and `find_nearest` now pass `sim.crowdPenaltyForNode` into A*
  (active in `low_crowd` mode).
- New `get_crowd` tool: density band (quiet/moderate/busy/packed) + wait + T+15/T+30
  projection for a place or the busiest zones overall. Honest "simulated" flag.
- REST: `GET /api/crowd/:venueId/heatmap` (enriched with zone label/level/kind/centroid
  + phase + sim_minute) and `/zone/:zoneId`.
- Simulator starts on server boot when CROWD_SIM_ENABLED.

## Verified live (Qwen + real graph)
- "Is the 100 Concourse food area busy? Go now or wait?" → get_crowd → reply:
  "moderate now, projected packed in 15 min, go now for quickest service" AND it
  spontaneously disclosed "Crowd data is simulated for this preview" — ADR 0005
  honesty, unprompted.

## Test state
- 13 backend tests pass (6 A* + 7 crowd). Both workspaces typecheck clean.

## Follow-ups (not blockers)
1. `get_crowd` fuzzy place→zone match is coarse ("food area" matched the concourse
   zone, not food). Improve label resolution: match zone kind keywords
   (food/restroom/gate) + level before falling back.
2. Firestore not wired — crowd is served over REST for now (frontend will poll).
   Direct Firestore subscribe (architecture.md) needs a Firebase project; deferred.
3. Crowd predictions not yet surfaced to the concierge for proactive nudges — that's
   the decision-support feature (later day).

## Next
D3.4/D3.5 — the visible frontend: SVG floor plan rendering an A* route + the crowd
heatmap layer (poll /api/crowd/heatmap). This is the most demo-critical UI.
